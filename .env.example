# ── LLM ──────────────────────────────────────────────────────────────
GROQ_API_KEY=votre_cle_groq_ici



# Claude — pour la démo finale (optionnel)
# ANTHROPIC_API_KEY=sk-ant-xxxxxxx

# ── Modèle Groq (optionnel — défaut : llama-3.1-8b-instant) ──────────
# GROQ_MODEL=llama-3.3-70b-versatile

# ── MCP Server ────────────────────────────────────────────────────────
# MCP_URL=http://localhost:8000/call
# MCP_TIMEOUT_HEAVY=600
# MCP_TIMEOUT_LIGHT=60

# ── Chemins (optionnel) ───────────────────────────────────────────────
# RUNS_DIR=runs
# UPLOADS_DIR=uploads

# ── Pipeline ──────────────────────────────────────────────────────────
# DEVOPS_MAX_RETRIES=2
